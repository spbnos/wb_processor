# 🧠 WB OS — MASTER PROMPT v5.0 (расширение поверх v4.0)
> Обновлён: 2026-06-23. Параллельный трек к v4.0.
> **v4.0 (SCOS) остаётся приоритетом №1.** Этот документ — Трек "WB OS" (приоритет №2),
> запускается по явному указанию пользователя, не блокирует SCOS.
> Всегда читать оба файла: `MASTER_PROMPT_v4.0.md` (SCOS) + этот файл (WB OS) в начале чата.
>
> ⚠️ **ВАЖНО**: этот файл должен лежать в `wb_platform/MASTER_PROMPT_v5.0.md` на GitHub.
> Если его там нет — он не сохранился при предыдущей сессии. Восстановлен 2026-06-23
> из истории чата после сброса контейнера. **Всегда коммитить этот файл в репозиторий
> сразу после получения, не просто скачивать локально** — иначе следующая сессия
> потеряет прогресс журнала находок.

---

## 🎯 ЦЕЛЬ ТРЕКА

Построить **полную операционную систему управления магазином(ами) WB** —
не просто аналитику (это уже есть в `wb_processor`), а замкнутый контур:

```
ДАННЫЕ (WB API + отчёты) → CANONICAL REGISTRY → DATA WAREHOUSE
   → ДАШБОРДЫ → AI-АНАЛИЗ → АВТОМАТИЗАЦИИ (правила) → ДЕЙСТВИЯ (через API обратно в WB)
```

### Жёсткое ограничение (от пользователя, обязательно)
**Запрещено придумывать**: отчёты, API-методы, поля данных, бизнес-процессы WB.
Если что-то не подтверждено официальной документацией dev.wildberries.ru —
помечать как **«ГИПОТЕЗА — требует проверки в Swagger»**, не выдавать за факт.

### Источник истины
- `https://dev.wildberries.ru/openapi/api-information` (и подкатегории: Content,
  Prices and Discounts, Marketplace, Statistics, Supplies, Analytics, Promotion,
  Feedbacks and Questions, Reports, Documents, Returns, Tariffs, Seller Info, Chat)
- Уже выгруженное в репозитории: `WB_API/wb_registry.json`, `WB_API/refs.json`,
  `WB_API/wb_products_openapi.json`, `WB_API/endpoints.txt` — **50 эндпоинтов**,
  подтверждено Шагом 1.1: это **100% полная спецификация `02-products.yaml`**
  ("Работа с товарами") официального API, не узкое подмножество.
- Вторичный источник для быстрой навигации по структуре всего API (не панацея,
  финальная сверка полей — всегда по dev.wildberries.ru):
  `github.com/eslazarev/wildberries-sdk` — автогенерируемое SDK, синхронизируется
  с офиц. OpenAPI несколько раз в неделю (см. его CHANGELOG.md).
- При сомнении в поле/методе — **искать в вебе** официальную страницу
  dev.wildberries.ru, не утверждать из памяти.

### Фундамент (не дублировать, расширять)
Этот трек **строится поверх** существующего `wb_processor`:
- `classification/canonical_report_registry.py` — уже 11 типов файловых отчётов
- `parsers/domain/*` — уже 11 парсеров
- `api/routes/{analytics,products,commissions}.py` — уже есть аналитика/матрица/комиссии
- `data/loaded/*.json` — уже файловое хранилище (RAW≈STAGING смешаны)
- Это соответствует **ЭТАПУ 3 (Data Warehouse)** ниже, но **только слою RAW/STAGING
  для файловых выгрузок**. Слоя CORE/MARTS и live-API-синка пока нет — это и есть
  предмет этого трека.

---

## 📐 ЭТАПЫ (зафиксированы из ТЗ пользователя, далее разбиваются на шаги)

| Этап | Название | Статус |
|---|---|---|
| 1 | Полный аудит данных WB (реестр сущностей + полей + API) | 🔄 в работе: 1.1 ✅ 1.2 ✅ 1.3 ✅ 1.4 🔜 |
| 2 | Canonical Semantic Registry (единые имена полей/сущностей) | ⏳ |
| 3 | Data Warehouse (RAW → STAGING → CORE → MARTS) | ⏳ частично есть RAW/STAGING |
| 4 | Карта процессов продавца (контент/цены/остатки/поставки/реклама) | ⏳ |
| 5 | Управленческие дашборды (экраны, метрики, фильтры, действия) | ⏳ частично есть (Analytics.tsx, ProductMatrix.tsx) |
| 6 | AI Layer (аналитик, контент-менеджер, рекламщик, категорийный менеджер) | ⏳ |
| 7 | Автоматизации (ЕСЛИ-ТО правила) | ⏳ пересекается с SCOS engine (v4.0 Этап SCOS-3) |
| 8 | Multi-Account (мультикабинетность) | ⏳ пересекается с SCOS-7 (multi-tenant) |
| 9 | API Registry (полный каталог методов WB) | 🔜 входит в Этап 1 |
| 10 | Roadmap (MVP/V2/V3 + ROI + риски) | ⏳ финальный документ этапа |

**Важно про пересечения с v4.0 (SCOS):** Этапы 6 (AI Рекламщик/автозаказ-смежные
части), 7 (автоматизации остатков) и 8 (multi-tenant) **не дублируют** SCOS —
SCOS — это конкретно автозаказы поставщикам. WB OS — это надстройка над ВСЕМИ
процессами продавца. Где пересекается (например, правило "остаток < X →
создать поставку") — реализация **переиспользует** SCOS engine
(`scos/apps/api/src/engine/*` из v4.0), а не пишется заново.

---

## 🪜 РАЗБИВКА ЭТАПА 1 НА ШАГИ

- **Шаг 1.1** ✅ — Реестр категорий официального API (14 спецификаций).
- **Шаг 1.2** ✅ — Сущность «Товары» (карточки, Content API).
- **Шаг 1.3** ✅ — Сущность **Остатки/Склады/Поставки** — выполнено.
- **Шаг 1.4** — Сущность **Заказы/Продажи/Возвраты** (Statistics + Returns API).
- **Шаг 1.5** — Сущность **Реклама** (Promotion API).
- **Шаг 1.6** — Сущность **Цены и скидки + Финансы** (Prices&Discounts + Reports + Documents).
- **Шаг 1.7** — Сводная таблица: Сущность × Источник × Покрытие × Приоритет.

---

## ⚠️ ПРАВИЛА ВЫПОЛНЕНИЯ (специфично для этого трека)

1. **Каждый шаг = отдельный ответ.** Не пытаться закрыть Этап 1 целиком за одно
   сообщение — дробить ещё мельче при необходимости.
2. **Перед фактами по API — web_search/web_fetch** dev.wildberries.ru. Не отвечать
   из памяти про конкретные поля/лимиты без проверки.
3. **Если страница dev.wildberries.ru недоступна** (бывает 498/403 при прямом fetch) —
   использовать web_search по конкретной подкатегории, затем точечный web_fetch
   найденного URL, или сверяться с уже выгруженными `WB_API/refs.json`/`wb_registry.json`
   (это первоисточник, выгруженный пользователем напрямую со Swagger WB).
4. **Каждый найденный факт — сверять с `WB_API/wb_registry.json` и `refs.json`**:
   это уже частично выгружено пользователем, не дублировать работу, а дополнять.
5. **Не трогать существующий рабочий код** на этапе 1 — это исследовательский этап.
6. **Все находки фиксировать в этот файл**, дополнять журнал ниже по мере прохождения
   шагов. **И сразу выкладывать пользователю для коммита в репозиторий** — не только
   показывать в чате, иначе прогресс теряется при сбросе сессии (как уже произошло
   один раз 2026-06-23).

---

## 📝 ЖУРНАЛ НАХОДОК ЭТАПА 1

### ШАГ 1.1 — Реестр категорий официального API ✅ ВЫПОЛНЕН (2026-06-23)

**Источник истины найден и проверен**: `github.com/eslazarev/wildberries-sdk` —
SDK-клиенты, автоматически пересобираемые при изменении официальных OpenAPI WB.

**ПОПРАВКА к предыдущему предположению**: `WB_API/wb_registry.json` (50 методов)
— это не узкое подмножество, а **100% полное покрытие** спецификации
`02-products.yaml` ("Работа с товарами") — 49 из 49 методов совпали 1:1.

#### Полный реестр 14 официальных спецификаций

| № | Спецификация | Назначение | Методов | Статус |
|---|---|---|---|---|
| 1 | `01-general.yaml` | Общее: пользователи, инфо о продавце, ping, новости | 7 | ❌ не выгружено |
| 2 | `02-products.yaml` | Товары: карточки, цены/скидки, остатки FBS, справочники, медиа, ярлыки | 49 | ✅ **100% есть** |
| 3 | `03-orders-fbs.yaml` | Заказы FBS: сборочные задания, поставки, стикеры | 33 | ❌ не выгружено |
| 4 | `04-orders-dbw.yaml` | Заказы DBW (доставка WB от склада продавца) | 16 | ❌ не выгружено |
| 5 | `05-orders-dbs.yaml` | Заказы DBS (доставка силами продавца) | 28 | ❌ не выгружено |
| 6 | `06-in-store-pickup.yaml` | Заказы "Самовывоз" | 21 | ❌ не выгружено |
| 7 | `07-orders-fbw.yaml` | Поставки FBW (на склады WB) | 7 | ❌ не выгружено — пересекается с файловыми парсерами |
| 8 | `08-promotion.yaml` | Реклама: ставки, кампании, статистика, акции | 34 | ❌ не выгружено — новая область |
| 9 | `09-communications.yaml` | Отзывы, вопросы, чаты | 24 | ❌ не выгружено |
| 10 | `10-tariffs.yaml` | Тарифы: коэффициенты, комиссии по категориям | 5 | ❌ не выгружено — критично для live-комиссий |
| 11 | `11-analytics.yaml` | Остатки WB, воронка продаж, поисковые запросы | 16 | ❌ не выгружено — новая область |
| 12 | `12-reports.yaml` | Отчёты: продажи/заказы/склады, платное хранение | 26 | 🟡 частично есть как файлы |
| 13 | `13-finances.yaml` | Баланс, документы, отчёт о реализации | 6 | 🟡 частично есть как файл |
| 14 | `14-wbd.yaml` | Wildberries Цифровой | — | ❌ низкий приоритет |

**Итог**: покрыта 1/14 полностью, 2/14 частично файлами, **11/14 отсутствуют**,
включая критичные: Реклама, Тарифы/комиссии, Аналитика/воронка/поиск, Заказы FBS/DBW/DBS.

---

### ШАГ 1.2 — Сущность «Товары»: поля карточки (Content API) ✅ ВЫПОЛНЕН (2026-06-23)

**Источник**: `WB_API/refs.json` → `/content/v2/get/cards/list` → `post` (первоисточник
со Swagger WB, выгружен пользователем напрямую).

#### Полная схема ответа `cards[]` (20 полей верхнего уровня)

| Поле | Тип | Описание |
|---|---|---|
| `nmID` | integer | Артикул WB |
| `imtID` | integer | ID для объединённых карточек |
| `nmUUID` | string (UUID) | Внутренний технический ID |
| `subjectID` | integer | ID предмета |
| `subjectName` | string | Название предмета |
| `vendorCode` | string | Артикул продавца |
| `brand` | string | Бренд |
| `title` | string | Наименование товара |
| `description` | string | Описание товара |
| `needKiz` | boolean | Требуется код «Честный знак» |
| `kizMarked` | boolean | Подтверждение нанесения маркировки |
| `photos` | array | Фото в 5 размерах |
| `video` | string | URL видео |
| `wholesale` | object | Оптовая продажа |
| `dimensions` | object | Габариты+вес с упаковкой |
| `characteristics` | array | Произвольные атрибуты предмета |
| `sizes` | array | Размеры товара (см. ниже) |
| `tags` | array | Ярлыки |
| `createdAt`/`updatedAt` | string | Даты |

**`sizes[]` — КРИТИЧНО**: `chrtID`, `techSize`, `wbSize`, `skus[]` (баркоды
привязаны к РАЗМЕРУ, не к товару целиком).

#### Сопоставление с нашим `product_catalog_parser.py` (Excel-парсер)

| Наше поле | WB API эквивалент | Совпадает? |
|---|---|---|
| `sku_id` | `nmID` | ✅ |
| `seller_article` | `vendorCode` | ✅ |
| `barcode` | `sizes[].skus[]` | ⚠️ структурный разрыв — у нас 1 баркод/товар, у WB баркод/размер |
| `product_name` | `title` | ✅ |
| `cost_price` | — нет в API вообще | ❌ легитимно эксклюзивно файловое поле |
| `brand` | `brand` | ✅ |
| `category`/`subject` | `subjectName`/`subjectID` | 🟡 у нас нет числового `subjectID` — нужен для join с тарифами |
| габариты | `dimensions.*` | ✅ точное совпадение |
| `color`/`material`/`country_origin` | `characteristics[]` | 🟡 у WB это гибкий массив, не колонки |
| — | `nmUUID`,`imtID`,`chrtID`,`needKiz`,`tags`,`updatedAt` | ❌ 9 полей отсутствуют у нас |

#### Выводы Шага 1.2
1. `cost_price` остаётся легитимно эксклюзивно файловым источником.
2. **`subjectID` нужно начать сохранять** уже сейчас — без него невозможен точный
   join с `10-tariffs.yaml` (тарифы оперируют ID, не текстом категории).
3. **Баркод-на-размер** — структурный разрыв модели для одежды/обуви, заложить
   в Canonical Registry (Этап 2) подсущность `ProductSize`.
4. **`characteristics[]`** — модель "ключ-значение", а не жёсткие колонки.
5. **9 полей карточки полностью отсутствуют**, важнейшие: `imtID` (агрегация
   объединённых карточек для Этапа 5), `tags`, `updatedAt`.

**Следующий шаг (1.3)**: сущность «Остатки/Склады/Поставки».

---

### ШАГ 1.3 — Сущность «Остатки/Склады/Поставки» ✅ ВЫПОЛНЕН (2026-06-23)

**Источник**: `WB_API/wb_products_openapi.json` (полный документ с `components/schemas`,
первоисточник со Swagger) для FBS-методов из `02-products.yaml`; web_search по
`eslazarev/wildberries-sdk` и `Dakword/WBSeller` для методов `07-orders-fbw.yaml`
(этой спецификации нет в нашей выгрузке вообще — собрано из вторичных источников,
**требует точечной проверки на dev.wildberries.ru перед написанием кода**).

#### 1.3.А — Остатки FBS: `/api/v3/stocks/{warehouseId}` (есть в `02-products.yaml`, ✅ выгружено)

Ключевая находка — **операционная единица здесь `chrtId` (ID размера), не `nmID`
и не баркод**:
- `POST .../stocks/{warehouseId}` (запрос остатков) — body: `{chrtIds: [int]}`
- `PUT .../stocks/{warehouseId}` (обновить) — body: `{stocks: [{chrtId, amount}]}`,
  max 1000 позиций за запрос, `amount` ≤ 100000
- `DELETE .../stocks/{warehouseId}` — body: `{chrtIds: [int]}`

**Это подтверждает находку Шага 1.2** (`sizes[].chrtID`) — `chrtId` это сквозной
идентификатор, который связывает карточку (Content API), остатки FBS и (см. ниже)
сборочные заданий FBS. **У нас в системе `chrtId` не сохраняется вообще ни в одном
парсере** — это пробел, который заблокирует прямую интеграцию с FBS-остатками,
если она понадобится.

#### 1.3.Б — Склады продавца (FBS): `/api/v3/warehouses` (✅ выгружено)

Схема `Warehouse` (из `components/schemas`, точная, с офиц. enum-описаниями):

| Поле | Тип | Описание |
|---|---|---|
| `id` | integer | ID склада продавца |
| `name` | string | Название (свободный текст) |
| `officeId` | integer | ID склада WB, к которому привязан склад продавца |
| `cargoType` | enum(1,2,3) | 1=МГТ (малогабаритный), 2=СГТ (сверхгабаритный), 3=КГТ+ (крупногабаритный) |
| `deliveryType` | enum(1,2,3,5,6) | 1=FBS, 2=DBS, 3=DBW, 5=самовывоз (C&C), 6=экспресс-DBS |
| `isDeleting` | boolean | Признак удаления (склад скоро пропадёт из списка) |

**Важно для понимания доменной модели**: это **не склады WB**, а склады/точки
отгрузки САМОГО ПРОДАВЦА, через которые идёт FBS/DBS/DBW/самовывоз. Склады WB
(куда едет FBO) — отдельная сущность, см. 1.3.В.
`GET /api/v3/offices` — это именно склады WB (куда продавец может привязать
свой склад через `officeId`), также есть в нашей выгрузке.

#### 1.3.В — Поставки FBW (на склады WB): `07-orders-fbw.yaml` (❌ НЕ выгружено — собрано из вторичных источников, нужна проверка)

По данным `wildberries-sdk` (см. полный список выше) и `Dakword/WBSeller`,
спецификация покрывает два разных версионных пространства методов:

**`/api/v1/...` — информация для планирования поставок:**
- `GET /api/v1/acceptance/coefficients` — **коэффициенты приёмки** по складам
  (ключевое для расчёта: платность/доступность приёмки меняется по складам и
  датам в зависимости от загрузки — прямо нужно для Шага 1.6/Этапа SCOS-3
  юнит-экономики, в нашем текущем калькуляторе `WBCommissions.tsx` этого
  параметра нет, используется фиксированный тариф)
- `POST /api/v1/acceptance/options` — опции приёмки (какие типы упаковки/коробов
  принимает конкретный склад)
- `GET /api/v1/warehouses` — список складов WB **для целей FBW** (отличается от
  `/api/v3/offices` по версии пространства имён, требует проверки — возможно
  один и тот же справочник под разным путём, гипотеза, не факт)
- `POST/GET /api/v1/supplies`, `GET /api/v1/supplies/{ID}/goods`,
  `GET /api/v1/supplies/{ID}/package` — список/детали/товары/упаковка поставки
- `GET /api/v1/transit-tariffs` — транзитные тарифы между регионами

**`/api/v3/...` — управление поставками (более новая версия):**
- `GET/POST /api/v3/supplies`, `GET/DELETE /api/v3/supplies/{supplyId}`,
  `GET .../supplies/{supplyId}/barcode` (QR-код поставки),
  `PATCH .../supplies/{supplyId}/deliver` (передать в доставку)
- `GET /api/v3/passes/offices`, `PUT/DELETE /api/v3/passes/{passId}` — **пропуска
  на склады** (для физического проезда машины с поставкой)

**ГИПОТЕЗА, требует проверки**: судя по нумерации (`v1` для информации,
`v3` для управления поставками + отдельно `marketplace/v3` для привязки
сборочных заданий к поставке), это вся одна спецификация FBW/FBS-поставок,
но точное разделение `07-orders-fbw.yaml` vs `03-orders-fbs.yaml` по этим
методам **не подтверждено напрямую** — `wildberries-sdk` показывает общий
список без явной привязки метод→файл-спецификация. Уточнить на Шаге 1.4
при разборе заказов FBS, так как `/api/v3/supplies` тесно связаны со
сборочными заданиями (`orders`), которые точно относятся к `03-orders-fbs.yaml`.

#### Сопоставление с нашими файловыми парсерами

| Наш парсер | Что парсит | WB API эквивалент | Пересечение |
|---|---|---|---|
| `stocks_parser.py` (`warehouse_stocks`) | Остатки на складах **WB** (FBO) из Excel-отчёта, по столбцам-складам (Коледино, Электросталь и т.д.) | **Нет прямого аналога в `02-products.yaml`** — это FBO-остатки, отдаются через `11-analytics.yaml` (не выгружено) | ❌ нет пересечения с тем, что выгружено; источник API для FBO-остатков — отдельная неисследованная спецификация |
| `recommendations_parser.py` (`supply_recommendations`) | "Рекомендуем отгрузить" — рекомендации пополнения по регионам, из Excel | Вероятно эквивалент аналитического отчёта в `11-analytics.yaml` (раздел рекомендаций), НЕ из `07-orders-fbw` | ❌ нет пересечения; нужна проверка в Шаге 1.5/1.6 при разборе `11-analytics.yaml` |
| — (нет у нас) | — | `/api/v1/acceptance/coefficients` (коэфф. приёмки), `/api/v3/supplies` (создание поставки), `chrtId`-остатки FBS | ❌ **полностью новая функциональность**, нужна для SCOS Этап SCOS-2/SCOS-3 (v4.0) — это именно то, через что SCOS должен **создавать** реальные поставки, а не только рекомендовать |

#### Выводы Шага 1.3

1. **Наши текущие парсеры (`warehouse_stocks`, `supply_recommendations`) закрывают
   только FBO-аналитику из Excel-отчётов**, они НЕ читают и НЕ пишут в WB через
   API. Для SCOS (v4.0, Этап SCOS-2 "WB API Client") это значит: создание
   реальной поставки потребует **нового слоя интеграции**, файловые парсеры его
   не заменяют.
2. **`chrtId` — сквозной ключ, которого у нас нет нигде** (карточки, остатки FBS,
   вероятно и сборочные заданий FBS). Это нужно закладывать в Canonical Registry
   (Этап 2) как обязательное поле сущности `ProductSize`, выявленной в Шаге 1.2.
3. **Коэффициенты приёмки (`/api/v1/acceptance/coefficients`)** — конкретный,
   проверенный по нескольким независимым источникам метод, который должен войти
   в калькулятор юнит-экономики (наш `WBCommissions.tsx`/`api/routes/commissions.py`
   сейчас используют фиксированный тариф приёмки — это упрощение, которое можно
   будет заменить живыми данными).
4. **FBO-остатки и FBO-рекомендации (наши текущие парсеры) — для них нужен
   отдельный API-эквивалент**, который пока не идентифицирован точно (вероятно
   в `11-analytics.yaml`, не выгружено). Закрыть на Шаге 1.5/1.6, **не путать
   с FBS-остатками** (`chrtId`-based, выгружено в 1.3.А) — это разные домены WB.
5. Граница между `07-orders-fbw` и `03-orders-fbs` для методов поставок (`/api/v3/supplies`,
   `/api/v3/passes`) осталась как **гипотеза** — требует точечной сверки на
   dev.wildberries.ru в начале Шага 1.4 (заказы FBS), не блокирует прогресс,
   но не должна быть выдана за факт раньше срока.

**Следующий шаг (1.4)**: сущность «Заказы/Продажи/Возвраты» (Statistics + Returns +
FBS Orders API) — заодно уточнить гипотезу про границу FBW/FBS из п.5 выше.
